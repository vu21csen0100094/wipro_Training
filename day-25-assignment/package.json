{
  "name": "skillsphere-api",
  "version": "1.0.0",
  "description": "SkillSphere LMS Backend",
  "main": "server.js",

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "mocha --exit \"test/**/*.spec.js\"",
    "coverage": "nyc npm test",
    "check-coverage": "nyc check-coverage --lines 100 --statements 100 --functions 100 --branches 100",
    "ci-test": "npm run coverage && npm run check-coverage"
  },

  "dependencies": {
    "express": "^4.19.0",
    "compression": "^1.7.4"
  },

  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "supertest": "^7.0.0",
    "nyc": "^15.1.0",
    "nodemon": "^3.0.0",
    "sinon": "^15.2.0",
    "proxyquire": "^2.1.3"
  }
}
